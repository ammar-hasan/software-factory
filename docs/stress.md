# Stress report

| Field | Value |
| --- | --- |
| Generated by | `python scripts/run_stress.py` |
| Generated at | 2026-09-01 02:42 UTC |
| Scenarios | 6 |
| Surprises | none |

These ask what the trials do not: whether the factory keeps behaving when several things
happen at once, when the ledger is large, when a provider misbehaves, and when the text
coming back is hostile rather than merely wrong. The failures those find differ in kind — a
corrupt ledger, a lost entry, a number that was right at ten runs and wrong at ten thousand,
a crash that takes the process with it. None of them show up on a happy path, and all of
them are what an operator meets first.

Thresholds here are deliberately loose. They exist to catch a change that makes something
*orders* worse, not to police a few hundred milliseconds on a shared runner.

## S1 concurrent ledger

8 writers x 250 appends into one hash-chained ledger.

Ran in 2.1s.

| Fact | Value |
| --- | --- |
| entries | 2000 |

| Check | Result |
| --- | --- |
| every append is present | ok — expected 2000, found 2000 |
| sequence numbers are dense and unique | ok — 2000 distinct of 2000 |
| no writer overwrote another | ok |
| the hash chain verifies after concurrent writes | ok |

## S2 concurrent coordinators

6 work items run in parallel against one repository and one ledger.

Ran in 2.0s.

| Fact | Value |
| --- | --- |
| entries | 258 |
| items | 6 |
| reached_handoff | 6 |

| Check | Result |
| --- | --- |
| every work item reached a settled state | ok — 6 of 6 |
| each run got its own workspace | ok — 6 workspaces for 6 runs |
| the ledger verifies after concurrent runs | ok |
| every item's runs are recorded under its own subject | ok |

## S3 large ledger

50,000 entries, folded by metrics, the run index and the activity board.

Ran in 29.7s.

| Fact | Value |
| --- | --- |
| board_seconds | 0.15 |
| entries | 45000 |
| index_seconds | 0.07 |
| metrics_seconds | 0.16 |
| verify_seconds | 0.81 |

| Check | Result |
| --- | --- |
| the run count is right at scale | ok — expected 5000, got 5000 |
| metrics fold in under 20s | ok — 0.2s |
| the run index sees every run | ok — got 5000 |
| the index is bounded | ok — 250 rows |
| the index builds in under 20s | ok — 0.1s |
| the activity board rebuilds every item | ok — 5000 items |
| the board rebuilds in under 20s | ok — 0.1s |
| a 50,000-entry chain verifies | ok |

## S4 hostile output

Oversized, injected, secret-bearing and malformed model output.

Ran in 1.1s.

| Fact | Value |
| --- | --- |
| inputs | 6 |
| ledger_mb | 0.0 |

| Check | Result |
| --- | --- |
| every hostile input was handled rather than raising | ok — 6 of 6 |
| the ledger verifies after hostile input | ok |
| a secret echoed by the model is not written to the ledger | ok |
| a two-megabyte answer does not become an unbounded ledger | ok — 0.0 MB |

## S5 flaky provider

20 runs against a provider failing randomly, with a fixed seed.

Ran in 2.9s.

| Fact | Value |
| --- | --- |
| outcomes | external_dependency=18; gate_failed_terminal=2 |

| Check | Result |
| --- | --- |
| no exception escaped a run | ok — 0 escaped |
| every run ended with a recorded outcome | ok — 20 of 20 |
| the ledger verifies after chaos | ok |

## S6 memory volume

20,000 memories, then stats and a lane query from a cold store.

Ran in 10.7s.

| Fact | Value |
| --- | --- |
| load_seconds | 0.41 |
| query_seconds | 0.001 |
| write_seconds | 10.3 |

| Check | Result |
| --- | --- |
| every memory survived | ok — expected 20000, got 20000 |
| a 20,000-memory store loads in under 30s | ok — 0.4s |
| a lane query returns results | ok — 6667 memories |
| a lane query answers in under 5s | ok — 0.00s |

