# QA renders only: the same diagram at two resolutions in two themes, produced to be looked
# at once. `spec/` is deliberately NOT ignored -- those JSON files are the source the HTML
# and PNG are generated from, and committing the output while ignoring its input leaves a
# diagram nobody can regenerate from a fresh clone. The first version of this file made
# exactly that mistake.
*.visual-check.*
