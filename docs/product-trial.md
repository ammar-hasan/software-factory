# Product trial: building `jsonlint`

| Field | Value |
| --- | --- |
| Generated by | `python scripts/product_trial.py` |
| Provider | openai-compatible |
| Model | glm-5.3-flash |
| Steps | 6 |
| Reached handoff | 2 of 6 |
| Total tokens | 2,317,508 |

`live_trial.py` proves the factory can carry *one* work item through a real model. That is
the smallest honest claim and not the interesting one. A factory is a thing you use for
months, and what decides whether it is usable is the second, fifth and twentieth change.

So this builds a real product through a sequence of work items chosen to probe the places a
factory fails: a change too small to justify the ceremony, one too large to do at once, a
request with nothing checkable in it, a defect that must prove its own fix, a bug planted in
code the factory had already touched, and a refactor that must change nothing.

**Every step's expectation and falsifier were written before the run.** Deciding afterwards
what a run was testing is how every trial comes out a success.

| Step | Probes | Reached | Tests | Turns | Tokens | Seconds |
| --- | --- | --- | --- | --- | --- | --- |
| small-clear | a small, completely unambiguous change | BLOCKED (external_dependency) | — | 56 | 450,537 | 1715 |
| large-clear | a large change that touches an existing public API | BLOCKED (gate_failed_terminal) | — | 25 | 211,623 | 1183 |
| ambiguous | a request with no checkable acceptance criterion | BLOCKED (external_dependency) | — | 20 | 104,124 | 455 |
| defect-fix | the regression-proven gate on a genuine defect | BLOCKED (gate_failed_terminal) | — | 24 | 187,861 | 846 |
| planted-defect | recovery from a bug in code the factory has already touched | HANDOFF | pass | 43 | 251,513 | 767 |
| refactor | a change that must alter structure and nothing else | HANDOFF | pass | 94 | 1,111,850 | 2567 |

## small-clear — Report the version

**Probes.** a small, completely unambiguous change

**Expected.** reaches handoff cheaply, with a test

**Would be wrong if.** spending as much as the largest step, or blocking on a clarification nobody needs -- a factory whose floor cost is its ceiling cost is unusable for the changes people actually make most often

**Request.** Add a `--version` flag to the command line entry point that prints the package version from `jsonlint.__version__` and exits 0.

Reached **BLOCKED** after TRIAGE → DESIGN → BUILD → REVIEW.

Blocked: `external_dependency` — RemoteDisconnected: Remote end closed connection without response

Changed: `README.md`, `design/wi_79f4f0bda9d4__version_flag.md`, `jsonlint/__main__.py`, `pyproject.toml`, `tests/test_cli.py`

| Blocking gate | Outcome |
| --- | --- |
| calibration-present | fail |

| Stage | Turns |
| --- | --- |
| TRIAGE | 16 |
| DESIGN | 14 |
| BUILD | 18 |
| REVIEW | 8 |

Spend: 378,847 in, 71,690 out, 1715s.

## large-clear — Report where the error is

**Probes.** a large change that touches an existing public API

**Expected.** reaches handoff with the old signature intact and new tests covering both the bool path and the located error

**Would be wrong if.** breaking `validate`'s existing contract, or reaching handoff with the new surface untested -- both pass a test suite that only knows the old behaviour

**Request.** Validation currently answers only true or false. Change it to report, for invalid input, the line and column of the first syntax error and a message naming what was expected. Keep `validate` returning a bool for existing callers and add whatever new surface the richer result needs.

Reached **BLOCKED** after TRIAGE → DESIGN.

Blocked: `gate_failed_terminal` — Emit the calibration block required by the stage's output schema.

Changed: `DESIGN.md`, `TRIAGE.md`, `design_probe.py`

| Blocking gate | Outcome |
| --- | --- |
| calibration-present | fail |

| Stage | Turns |
| --- | --- |
| TRIAGE | 9 |
| DESIGN | 16 |

Spend: 166,099 in, 45,524 out, 1183s.

## ambiguous — Make the errors better

**Probes.** a request with no checkable acceptance criterion

**Expected.** blocked for clarification, or a change whose acceptance criteria the factory wrote down and can be judged against

**Would be wrong if.** quietly guessing and reaching handoff -- an unfalsifiable change nobody asked for, which is the single most expensive thing an autonomous factory can do

**Request.** The errors could be better. Improve them.

Reached **BLOCKED** after TRIAGE → DESIGN.

Blocked: `external_dependency` — https://api.z.ai/api/coding/paas/v4/chat/completions returned 500: Operation failed

Changed: `spikes/error_api.py`

| Blocking gate | Outcome |
| --- | --- |
| calibration-present | fail |

| Stage | Turns |
| --- | --- |
| TRIAGE | 14 |
| DESIGN | 6 |

Spend: 87,042 in, 17,082 out, 455s.

## defect-fix — Duplicate keys are accepted silently

**Probes.** the regression-proven gate on a genuine defect

**Expected.** a test that fails at the parent commit for the right reason, then passes -- and no handoff without one

**Would be wrong if.** reaching handoff with a test that passed before the change, which would mean the keystone gate can be satisfied by a test that proves nothing

**Request.** `validate` accepts an object with duplicate keys, which is legal JSON but almost always a mistake in a config file. Reject it, and say which key was duplicated.

Reached **BLOCKED** after TRIAGE → BUILD.

Blocked: `gate_failed_terminal` — Run the new test against the parent commit.

Changed: `BUILD.md`, `TRIAGE.md`, `jsonlint/core.py`, `tests/test_core.py`

| Blocking gate | Outcome |
| --- | --- |
| regression-proven | fail |

| Stage | Turns |
| --- | --- |
| TRIAGE | 9 |
| BUILD | 15 |

Spend: 152,221 in, 35,640 out, 846s.

## planted-defect — Trailing commas are accepted

**Probes.** recovery from a bug in code the factory has already touched

**Expected.** finds the planted regression and fixes it without undoing earlier work

**Would be wrong if.** fixing the trailing comma by reverting the file to its original state, which passes the new test and silently deletes every previous change

**Request.** `validate` now accepts a trailing comma in an object, which is not valid JSON. Fix it so trailing commas are rejected, and keep the duplicate-key behaviour.

Reached **HANDOFF** after TRIAGE → BUILD → REVIEW → HANDOFF.

Changed: `jsonlint/core.py`, `review_probe.py`, `tests/test_core.py`

The product's own tests: **pass**

| Stage | Turns |
| --- | --- |
| TRIAGE | 7 |
| BUILD | 21 |
| REVIEW | 7 |
| HANDOFF | 8 |

Spend: 222,180 in, 29,333 out, 767s.

## refactor — Separate parsing from reporting

**Probes.** a change that must alter structure and nothing else

**Expected.** handoff with every existing test passing and no behaviour change

**Would be wrong if.** changing behaviour under cover of a refactor, which no test names

**Request.** The validation and the error formatting are in one function. Split them so the formatting can be tested on its own, without changing any behaviour.

Reached **HANDOFF** after TRIAGE → DESIGN → BUILD → REVIEW → HANDOFF.

Changed: `equivalence_probe.py`, `jsonlint/core.py`, `spec_delta_wi_759813befe47.md`, `tests/test_core.py`

The product's own tests: **pass**

| Stage | Turns |
| --- | --- |
| TRIAGE | 17 |
| DESIGN | 18 |
| BUILD | 21 |
| REVIEW | 25 |
| HANDOFF | 13 |

Spend: 1,004,125 in, 107,725 out, 2567s.

